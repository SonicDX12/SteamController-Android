# Copy this file to keystore.properties and fill in your values.
# keystore.properties is gitignored — it MUST stay local.
#
# Generate the .jks first with: scripts/generate-keystore.{ps1,sh}

storeFile=keystore/release.jks
storePassword=CHANGE_ME
keyAlias=steamcontroller
keyPassword=CHANGE_ME
